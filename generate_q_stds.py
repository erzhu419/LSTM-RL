import torch
import numpy as np
import os
import glob
import sys

# Append the environment directory to path
sys.path.append('/home/erzhu419/mine_code/LSTM-RL')

device = torch.device("cuda:0" if torch.cuda.is_available() else "cpu")

def main():
    pass

if __name__ == '__main__':
    main()
